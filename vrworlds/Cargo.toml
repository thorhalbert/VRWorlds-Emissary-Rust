[package]
name = "vrworlds"
version = "0.1.0"
authors = ["Thor Halbert <thorhalbert@gmail.com>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
protobuf = { version = "2", features = ["with-bytes"] }